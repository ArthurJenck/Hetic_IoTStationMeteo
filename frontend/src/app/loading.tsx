import { CenteredLayout } from "@/components/layout/centered-layout";

export default function Loading() {
    return (
        <CenteredLayout>
            Loading...
        </CenteredLayout>
    );
}